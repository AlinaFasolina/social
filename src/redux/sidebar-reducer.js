let initialState = {
  friends: [
    { id: 1, name: "Nastya" },
    { id: 2, name: "Stasya" },
    { id: 2, name: "Dave" },
  ],
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};
export default sidebarReducer;
